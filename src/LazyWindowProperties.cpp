#include "LazyWindowProperties.h"

namespace LWProperties
{
    Vector<int> graphOffset = { 0,0 };
    int logicalHeightBase = 2000;

    float zoom = 1.0f;
    float zoomStep = 1.1f;
    float zoomMin = 0.2f;
    float zoomMax = 5.0f;
}